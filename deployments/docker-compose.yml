version: '3'
services:
  etcd:
    image: bitnami/etcd:latest
    environment:
      - ALLOW_NONE_AUTHENTICATION=yes
    ports:
      - "2379:2379"
  kafka:
    image: apache/kafka:latest
    ports:
      - "9092:9092"
